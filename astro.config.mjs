import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://richardgoudelin.github.io/richardgoudelin.github.io',  // ou + '/portfolioF' si tu veux
  //base: 'richardgoudelin.github.io',                      // <-- note bien le slash final
});
